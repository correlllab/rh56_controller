# Tests

This directory is reserved for automated tests that are safe to run without
real robot hardware.

Run the current suite with:

```bash
uv run --extra test pytest
```

Manual serial, RH56, UR5, and experiment scripts live under `tools/`.
